# Fracode MCP — ядро. Только то, без чего память не поднимется.
#
# Здесь НЕТ chromadb / mem0ai / matplotlib. Раньше они лежали в одном файле с
# ядром, из-за чего установка ради одного MCP-сервера тянула полгигабайта.
# Они нужны только для сравнительного бенча — см. requirements-bench.txt.

fastapi>=0.110
uvicorn>=0.29
numpy>=1.26
torch>=2.2
tokenizers>=0.19
mcp>=1.0            # FastMCP; импорт адаптирован под mcp>=1.10 (см. frakod_mcp.py)
pydantic>=2.0
